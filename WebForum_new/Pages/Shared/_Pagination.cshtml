@model WebForum_new.Models.PaginationInfo

<nav aria-label="Page navigation">
    <ul class="pagination">
        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                <a class="page-link" href="?id=@i">@i</a>
            </li>
        }
    </ul>
</nav>
